import { redirect, useParams } from "react-router-dom";

export const DeleteEvent = ({ params }) => {
  console.log(params);
  //  const eventDeleting = fetch(`http://localhost:3000/events/${params.eventId}`)
  //     method: "DELETE",

  //  redirect(`/events`);
};
